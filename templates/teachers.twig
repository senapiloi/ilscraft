<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta charset="utf-8" />
    <title>Events</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
    <meta name="referrer" content="origin-when-cross-origin" />
    <link rel="stylesheet" type="text/css" href="assets/css/style.css">
</head>
<body class="ltr bg-[#2DAF90] place-content-center">
    <div class="flex mb-4">
  <div class="w-1/2 bg-gray-400 h-12"></div>
  <div class="w-1/2 bg-gray-500 h-12"></div>
</div>
   
    <div class="container mx-auto mb-4 px-44">
         <div class="title pt-28">
            <h1 class="mb-8 text-4xl text-white">Our Teachers</h1>
        </div>    
        <div class="flex flex-wrap px-5">
            {% for entry in craft.entries.section('teachers').order('postDate asc').limit(8) %}
            <div class="lg:w-1/2 md:w-1/2 mb-8"> 
                <div class="flex mt-2 items-center">
                  <div class=""><img class="rounded-lg" src="{{entry.image[0].url}}"></div>
                  <div class="px-2 m-auto"><p class=" font-medium text-white">{{ entry.title }}</p>
                    <p class="text-sm font-normal text-white"> {{ entry.position }}</p></div>
                </div>          
                <!-- <div class="rounded-full">
                    <img class="rounded-full" src="">
                </div>
                <div class="bg-white px-6">
                    <div class="pt-6">
                        <h2 class="text-sm text-indigo-600">
                            {{ entry.title }}                
                        </h2>
                    </div>
                    <div class="pt-2 pb-3">
                         <h1 class="text-xl">
                            {{ entry.position }}
                        </h1>
                    </div>  
                    <p class="hidden text-base text-gray-500">
                       {{ entry.description }}
                    </p>
                </div> -->
            </div>
            {% endfor %}
            <div class="w-full"><button>View More</button></div>
        </div>
    </div>
</body>
</html>
